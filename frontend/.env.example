VITE_API_URL=https://your-backend-url.onrender.com/api
```

**Save**

---

#### **D) Update .gitignore**

Make sure both `.env` files are ignored!

**Backend .gitignore** (if not already there):
```
node_modules/
.env
.DS_Store
```

**Frontend .gitignore** (if not already there):
```
node_modules/
.env
dist/
.DS_Store